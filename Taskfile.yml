version: "3"

dotenv: [".env"]

tasks:
  default:
    desc: List available tasks
    cmds:
      - task --list-all

  generate:sqlc:
    desc: Generate sqlc code for Postgres token store
    dir: tokenstore/postgres
    cmds:
      - sqlc generate

  test:
    desc: Run all tests
    cmds:
      - go test -v -count=1 ./...

  test:cover:
    desc: Run tests with coverage summary
    cmds:
      - go test -coverprofile=coverage.out -count=1 ./...

  test:cover:html:
    desc: Generate HTML coverage report and open in browser
    cmds:
      - go test -coverprofile=coverage.out -count=1 ./...
      - go tool cover -html=coverage.out -o coverage.html
      - open coverage.html

  test:postgres:
    desc: Run Postgres integration tests (uses testcontainers)
    cmds:
      - go test -v -count=1 ./tokenstore/postgres/...

  clean:
    desc: Remove build artifacts and local data
    cmds:
      - rm -rf bin/
      - rm -rf .data/

  tidy:
    desc: Tidy go modules
    cmds:
      - go mod tidy
