version: "2"
sql:
  - engine: "postgresql"
    queries: "queries.sql"
    schema: "migrations/"
    gen:
      go:
        package: "pgstore"
        out: "pgstore"
        sql_package: "pgx/v5"
        emit_json_tags: false
        emit_exact_table_names: true
